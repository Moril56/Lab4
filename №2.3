#include <stdio.h>

int main() {
    int sum = 0;
    int i = -20;

    do {
        if (i > 0) {
            sum += i;
        }
        i++;
    } while (i <= 10);

    int square = sum * sum;
    printf("Квадрат суми всіх додатніх чисел у діапазоні [-20; 10]: %d\n", square);

    return 0;
}
