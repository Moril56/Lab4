#include <stdio.h>

int main() {
    int sum = 0;
    int i;

    for (i = -20; i <= 10; i++) {
        if (i > 0) {
            sum += i;
        }
    }

    int square = sum * sum;
    printf("Квадрат суми всіх додатніх чисел у діапазоні [-20; 10]: %d\n", square);

    return 0;
}
